[binaries]
c = 'aarch64-linux-musl-gcc'
cpp = 'aarch64-linux-musl-g++'
ar = 'aarch64-linux-musl-ar'
strip = 'aarch64-linux-musl-strip'
ranlib = 'aarch64-linux-musl-ranlib'
pkgconfig = 'pkg-config'

[properties]
needs_exe_wrapper = true

# Avoid GCC runtime dependencies (crtbeginS.o, crtendS.o, libgcc, libgcc_eh)
# Use -nostdlib and manually specify musl's static libc
# Note: Path will be resolved relative to musl cross-compiler toolchain
c_link_args = ['-nostdlib', '-L/usr/aarch64-linux-musl/lib', '/usr/aarch64-linux-musl/lib/libc.a']

[host_machine]
system = 'linux'
cpu_family = 'aarch64'
cpu = 'aarch64'
endian = 'little'
